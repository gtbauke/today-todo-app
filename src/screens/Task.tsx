import React from 'react';
import { Text, View } from 'react-native';

export const Task: React.FC = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Hello, World! (Task Screen)</Text>
    </View>
  );
};
